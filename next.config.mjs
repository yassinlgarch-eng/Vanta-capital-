/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ملاحظة: للنشر على GitHub Pages كـ static export، فعّل التالي:
  // output: 'export',
  // images: { unoptimized: true },
  // basePath: '/vanta-capital',
};

export default nextConfig;
